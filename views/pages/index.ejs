<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/styles.css">
    <title>Todo List</title>
</head>
<body>
    <div class="center">
        <% if (currentDay === "Saturday" || currentDay === "Sunday" ) { %>
            <h1 style="color: purple;">Today, It's <%= currentDay %></h1>
        <% } else { %>
            <h1 style="color: blue;">Today, It's <%= currentDay %></h1>         
        <% } %>
    </div>
</body>
</html>